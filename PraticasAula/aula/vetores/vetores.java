package PraticasAula.aula.vetores;

public class vetores {
    public static void main(String[] args) {
	    int[] numeros={2,4,19,7,3};
	    int soma=0,impar,maiorNumero=0;
	    int cont;
	    for(cont=0;cont<5;cont++){
	    /* Somando numeros do vetor 
	    soma = soma+numeros[cont];
		System.out.printf("Numero na memoria: %s \nSoma atual: %d\n", numeros[cont],soma);
		*/
		/* Selecionando apenas os numeros impares
		if(numeros[cont]%2!=0){
		    impar = numeros[cont];
		    System.out.printf(" %d\n", impar);
		}
		*/
		/* Selecionando o maior numero
		if(maiorNumero==0){
		    maiorNumero = numeros[cont];
		}
		if(maiorNumero<numeros[cont]){
		    maiorNumero = numeros[cont];
		    System.out.printf("Maior numero Atual: %d\n", maiorNumero);
		}
		*/
		
		
	    }
	}
    
}
